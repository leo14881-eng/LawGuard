# LawGuard 项目唯一事实源

本文件是法护（LawGuard）项目的唯一事实源（Source of Truth）。任何与本文件冲突的历史记录、注释或口头约定均以本文件为准。

## 1. 项目基本信息

- 项目中文名：法护
- 项目英文名：LawGuard
- 项目性质：免费、公益、独立、非商业的刑事诉讼权利普法平台
- 代码仓库根目录：`D:\SOFT\LawGuard`
- 前端目录：`D:\SOFT\LawGuard\web`
- 版本：V1（首个可用版本，范围见第 6 节）

## 2. 核心使命

帮助被讯问、被追诉人员及其家属，在法律框架内了解法定权利、基本程序、文书核对方法和官方救济渠道。
目标是提供一般性、通俗化的普法信息，降低信息不对称，而不是替代专业法律服务。

## 3. 公益与非官方性质

法护（LawGuard）：

- 不属于政府机关；
- 不属于公安机关、检察院、法院；
- 不属于律师事务所；
- 不属于法律援助机构；
- 不提供具体案件分析；
- 不提供辩护策略；
- 不判断具体机关或人员是否违法；
- 不替代执业律师；
- 不收集案件信息；
- 不允许用户上传材料；
- 不提供在线咨询；
- 不提供 AI 聊天；
- 不收费；
- V1 不接受捐款。

## 4. 产品边界

法护只提供**一般性**权利说明、程序指引、文书核对方法和官方救济渠道信息，不针对任何具体案件、具体机关或具体人员发表判断或意见。所有法律相关内容必须来自可核验的公开正式文本，无法核验时明确标注"待法律复核"，不得虚构。

## 5. 目标用户

- 被讯问、被采取强制措施或被追诉的当事人；
- 当事人的家属；
- 需要了解刑事诉讼一般性程序与权利常识的公众。

## 6. V1 功能范围

1. 首页
2. 刑事诉讼阶段导航
3. 权利知识卡片
4. 法律依据展示
5. 文书核对清单
6. 官方救济渠道
7. 本地全文搜索
8. 关于项目
9. 隐私说明
10. 使用边界说明
11. 法律来源与版本记录

## 7. V1 明确不做

- 不引入 Java / Spring Boot / Node.js 后端服务；
- 不引入 MySQL / Redis 等数据存储；
- 不做用户系统、登录注册；
- 不做表单提交、文件上传；
- 不做评论、论坛；
- 不做在线咨询、AI 聊天接口；
- 不接入数据统计 SDK、广告 SDK；
- 不引入 Tailwind CSS 或大型 UI 组件库；
- 不做个案分析或辩护策略建议；
- 不接受捐款。

## 8. 信息架构

刑事诉讼阶段（页面信息分类，**不代表对具体案件程序状态作出判断**）：

1. 被带走或被传唤后
2. 讯问过程中
3. 核对和签署文书时
4. 审查起诉阶段
5. 法院审判阶段
6. 判决后及社区矫正阶段

## 9. 页面清单

| 路径 | 页面 | 说明 |
| --- | --- | --- |
| `/` | 首页 | 首屏、阶段入口、核心功能区、官方渠道区、边界说明、页脚 |
| `/stages` | 刑事诉讼阶段导航 | 六个阶段列表，详情内容未完成时跳转 `/coming-soon` |
| `/documents` | 文书核对清单 | 文书核对方法框架，具体清单内容标注审核状态 |
| `/official-channels` | 官方救济渠道 | 12348、12309 等官方渠道说明与入口占位 |
| `/about` | 关于项目 | 项目定位、公益与非官方性质说明 |
| `/privacy` | 隐私说明 | 不收集用户数据的说明 |
| `/disclaimer` | 使用边界说明 | 不提供个案判断、不替代律师等边界声明 |
| `/legal-sources` | 法律来源与版本记录 | 内容依据的法律文件框架与更新记录 |
| `/coming-soon` | 统一占位页 | 尚未完成法律复核的内容统一跳转至此，不出现 404 |

## 10. 内容数据模型

V1 内容以本地 TypeScript 静态数据维护，位于 `web/src/data/`，不接入任何后端或数据库。

`stages.ts` 定义阶段数据结构：

```ts
interface Stage {
  id: string        // 阶段唯一标识
  order: number      // 展示顺序
  name: string        // 阶段名称
  summary: string      // 一句话通俗说明
  status: '待法律复核' | '已复核'  // 内容审核状态，V1 默认均为待法律复核
}
```

## 11. 法律内容审核状态

- 所有法律相关正式内容（法条引用、程序细节、文书清单具体项）在未经执业律师审核前，统一标注为"待法律复核"。
- 禁止仅凭模型记忆虚构法律条文、案号、机关名称或链接。
- 官方渠道链接在未核验前标注"待官方链接核验"，不得描述为已核验的正式官方链接。
- V1 当前所有内容状态：**待法律复核 / 待官方链接核验**（尚未引入任何经核验的正式法律条文或官方链接）。

## 12. UI 设计规范

- 整体风格：冷静、可信、克制、专业，不制造恐慌。
- 主色：深蓝色系（`--color-primary: #14335c`，深蓝强调 `#0b2545`）。
- 辅助色：浅蓝灰（`--color-surface: #eef2f7`）与白色（`--color-bg: #ffffff`）。
- 禁止使用：手铐、铁窗、血迹、警灯、法槌大图、恐怖或对抗性图片、大面积鲜红色、夸张阴影、炫酷动画。
- 移动端优先，适配 375px 宽度，桌面端内容最大宽度约 1200px，不出现横向滚动。
- 不使用外部图片依赖、不使用在线字体、不引入第三方统计脚本。
- 文字清晰、按钮可访问、对比度合理。

## 13. 技术架构

固定技术栈：

- Vue 3 + `<script setup lang="ts">`
- TypeScript
- Vite
- Vue Router（`createWebHistory`）
- 原生 CSS（设计变量 + 组件级样式，无 CSS 框架）
- 静态 TypeScript 内容数据（`web/src/data/`）
- Git 版本管理

V1 明确禁止引入的技术见第 7 节。

## 14. 目录结构

```
D:\SOFT\LawGuard
├─ CLAUDE.md
├─ LAWGUARD_SOT.md
└─ web/
   ├─ index.html
   ├─ src/
   │  ├─ main.ts
   │  ├─ App.vue
   │  ├─ style.css
   │  ├─ router/
   │  │  └─ index.ts
   │  ├─ data/
   │  │  └─ stages.ts
   │  ├─ components/
   │  │  ├─ AppHeader.vue
   │  │  ├─ AppFooter.vue
   │  │  ├─ HeroSection.vue
   │  │  ├─ StageCard.vue
   │  │  ├─ FeatureCard.vue
   │  │  ├─ ChannelCard.vue
   │  │  └─ NoticeBanner.vue
   │  └─ views/
   │     ├─ HomeView.vue
   │     ├─ StagesView.vue
   │     ├─ DocumentsView.vue
   │     ├─ OfficialChannelsView.vue
   │     ├─ AboutView.vue
   │     ├─ PrivacyView.vue
   │     ├─ DisclaimerView.vue
   │     ├─ LegalSourcesView.vue
   │     └─ ComingSoonView.vue
   └─ public/
      └─ favicon.svg
```

## 15. 安全与隐私规则

- 不收集任何用户案件材料、个人身份信息；
- 不提供表单提交、文件上传、登录注册；
- 不接入数据统计 SDK、广告 SDK 或任何第三方追踪脚本；
- 不使用 Cookie 进行用户追踪；
- 所有内容均为静态展示，无用户数据在客户端或服务端留存。

## 16. 开发与验收规则

- 每次修改前先阅读本文件；
- 每次完成开发后更新第 17 节"当前开发进度"；
- 修改后必须执行 `npm run build`（在 `web/` 目录），确保类型检查与构建均通过；
- 若存在 lint/typecheck 命令需一并执行；
- 验收需确认：TypeScript 无错误、页面可构建、路由刷新不报错、375px 宽度无横向滚动、
  全部页面使用简体中文、不存在表单、不存在用户数据采集、不存在统计 SDK、
  不存在 Vite 默认脚手架内容、不存在被描述为"正式官方"却未核验的链接。

## 17. 当前开发进度

- [x] 项目根目录 `CLAUDE.md` 按规则更新，写入 14 条开发规则。
- [x] `LAWGUARD_SOT.md` V1.0 完整编写。
- [x] 安装并配置 Vue Router，创建 9 个路由页面（含 `/coming-soon` 占位页）。
- [x] 清理 Vite 默认脚手架内容（HelloWorld、默认 hero 图、默认 favicon 等）。
- [x] 完成首页 V1 基础版本：顶部导航、首屏、六个阶段入口卡片、核心功能区、官方渠道区、内容边界区、页脚。
- [x] 完成 `/stages`、`/documents`、`/official-channels`、`/about`、`/privacy`、`/disclaimer`、`/legal-sources`、`/coming-soon` 页面框架，内容状态标注"待法律复核"或"待官方链接核验"。
- [x] `npm run build` 通过。

## 18. 下一步计划

- 引入本地全文搜索（V1 功能范围第 7 项），确定搜索索引方案（静态数据全文匹配）。
- 补充六个阶段的详情页正文内容，需先完成法律复核后由 `/coming-soon` 切换为正式内容。
- 补充文书核对清单的具体条目，需法律复核后发布。
- 核验 12348、12309 等官方渠道的官方链接与最新联系方式后更新"待官方链接核验"状态。
- 补充法律来源与版本记录页的具体法条引用（需可核验的正式文本来源）。

## 19. 重要决策记录

- 2026-07-26：确定 V1 固定技术栈为 Vue 3 + TypeScript + Vite + Vue Router + 原生 CSS + 静态数据，不引入后端、数据库、用户系统。
- 2026-07-26：确定路由采用 `createWebHistory`，尚未完成法律复核的阶段详情统一跳转 `/coming-soon`，不出现 404。
- 2026-07-26：确定 V1 阶段暂定为六个阶段（见第 8 节），仅作为页面信息分类，不代表对具体案件程序状态的判断。
- 2026-07-26：确定所有未经执业律师审核的法律内容统一标注"待法律复核"，官方链接未核验前统一标注"待官方链接核验"，不得虚构。
- 2026-07-26：完成 V1 首页与基础路由页面框架搭建。
