# LawGuard 项目唯一事实源

本文件是法护（LawGuard）项目的唯一事实源（Source of Truth）。任何与本文件冲突的历史记录、注释或口头约定均以本文件为准。

## P-1：非法律意见原则（最高优先级）

本章优先级高于本文件包括 P0 在内的其余所有章节，是法护（LawGuard）项目永久最高治理原则。

法护永远是公益普法平台：不是律师，不是法律咨询平台，不是案件分析平台，不是法律意见
提供者。

任何 AI（OpenAI、Claude Code 或未来接入本项目的其他模型）均不得：

- 判断某案件是否合法；
- 判断是否构成犯罪；
- 判断是否有罪；
- 判断可能会判几年；
- 判断警方、检察院、法院的具体行为是否合法；
- 判断某位律师的意见是否正确；
- 判断案件输赢；
- 推荐诉讼策略；
- 推荐辩护方案；
- 推荐举报方案；
- 推荐如何规避法律责任。

如果用户或任务内容涉及具体案件咨询，必须立即停止，统一回复：

> "LawGuard 不提供个案法律意见。如需法律帮助，请咨询执业律师或当地法律援助机构。"

Planner（OpenAI CTO 角色）与 Reviewer（评审器）在规划或评审任务时必须同时执行本规则：
任何涉及个案判断、诉讼/辩护/举报策略建议或规避法律责任建议的任务，一律判定为 `BLOCKED`
（Planner）或 `FAIL`（Reviewer），不得放行。

## P0：法律内容真实性原则（Legal Accuracy First）

本章优先级仅次于 "P-1：非法律意见原则"，高于本文件及 `CLAUDE.md` 中除 P-1 外的任何其他
条款。如与其他章节（P-1 除外）冲突，以本章为准。

补充：任何一句涉及法律事实的话，必须能够追溯到项目内已核验的官方来源；否则必须 `BLOCKED`，
不得生成，不得发布。

法护是公益普法平台。法律内容的准确性永远高于：

- 开发速度；
- 页面数量；
- 自动化程度；
- AI 生成效率。

**宁可缺失，不可错误。宁可返回 BLOCKED，不得猜测，不得编造。**

### P0.1 AI 行为规范

所有 AI（包括 OpenAI、Claude Code、以及未来接入本项目的任何模型）在处理法律内容时必须遵守：

1. 不得依据模型训练记忆新增法律事实。
2. 不得自行补充以下内容：法律条文、条文编号、权利和义务、法定期限、法律程序、适用条件、
   法律后果、司法解释、办案流程、救济渠道。
3. 如果没有可靠来源：必须停止，输出 `BLOCKED`，绝不能猜测。
4. 规划器（OpenAI CTO 角色）发现需要新增法律事实但没有可靠来源时，必须返回 `BLOCKED`
   任务，不得创建包含该法律内容的开发任务。
5. Claude Code 发现任务要求补充未经核验的法律内容时，必须停止实施该部分内容，并在
   执行摘要中明确报告 `BLOCKED` 及具体原因，不得为了完成任务而编造或推断。

### P0.2 法律来源要求

法护所有法律内容必须能够追溯到来源。

允许的来源仅限：

- 中华人民共和国法律（全国人大正式公布）；
- 最高人民法院；
- 最高人民检察院；
- 公安部；
- 国家监察委员会；
- 国务院正式法规；
- 其他明确批准进入项目的官方来源。

不得引用：AI 记忆、网络文章、自媒体、未核验博客、未确认 PDF、论坛内容。

### P0.3 开发规则

自动开发或人工开发过程中，如果需要新增法律内容：

1. 第一步：检查项目中是否已经存在可靠来源（见 P0.2）。
2. 存在可靠来源：允许引用，并按 P0.4 记录来源信息。
3. 不存在可靠来源：立即停止，返回 `BLOCKED`，不得继续开发、不得凭记忆或推测补全。

### P0.4 页面记录要求

以后涉及法律内容的页面，必须支持记录以下信息，并允许页面展示：

- 官方来源名称；
- 官方来源地址或项目内来源标识；
- 法律版本或施行状态；
- 最后核验日期。

### P0.5 Review Gate（评审阻断规则）

代码评审（包括 OpenAI 评审器与人工评审）新增以下阻断规则：

只要发现改动新增了法律事实，但没有对应的可核验来源，评审结论必须为 `FAIL`，不得 `PASS`。

### P0.6 未来更新规则

如果法律发生修订：自动开发系统不得自动修改已发布的法律内容，只能生成"待审核"任务，
等待人工确认后才能更新。

### P0.7 禁止事项

任何情况下：

- 不得为了完成任务而编造法律内容；
- 不得根据模型记忆补全文字；
- 不得自行推断法律程序；
- 不得推断司法实践；
- 不得推断期限；
- 不得推断适用条件；
- 不得使用自媒体、论坛、博客、模型记忆或无法确认真实性的资料作为法律依据；
- 不得为了通过验收而补写、推断或润色出新的法律结论。

### P0.8 最终原则

> LawGuard 的核心价值不是生成更多法律内容，而是保证每一句法律内容都真实、准确、可追溯。
> 宁可缺失，不可错误；宁可返回 BLOCKED，也不得猜测或编造。
>
> 任何一句涉及法律事实的描述，都必须能够追溯到官方来源。如果无法证明其来源，则不得发布。

此 P0 原则的优先级高于开发速度、页面数量、自动化程度和任务完成率，适用于本文件、
`CLAUDE.md`、`automation/prompts/planner_system.txt`、`automation/prompts/reviewer_system.txt`
以及全部未来接入本项目的开发流程。

## P1：官方优先原则

所有法律内容页面的呈现顺序必须遵循：

官方来源 → LawGuard 解释 → 辅助说明

不得出现 "AI 解释 → 官方来源" 这种顺序，即不得让 AI 生成的解释性文字先于、或替代对
官方来源本身的呈现。

以后所有涉及法律内容的页面必须支持记录并可展示：

- 官方来源；
- 法律版本；
- 最后核验日期。

（与 P0.4 页面记录要求一致，本章从内容呈现顺序角度作出补充规定。）

## P2：法律版本管理原则

如果检测到所引用法律的版本发生变化（新法公布、旧法废止、条文修订等）：

- 自动开发系统不得自动修改已发布的法律内容；
- 只能生成"待人工审核"任务，写明检测到的版本变化情况；
- 必须等待人工确认后才能更新页面内容；
- 不得自动覆盖原有已发布的正式内容。

（与 P0.6 未来更新规则一致，本章作为独立治理原则单列，便于规划器与评审器引用。）

## P3：产品级界面原则（Product UI First）

法护的前端页面必须达到成熟 SaaS 产品水平，不得做成临时后台、默认组件堆砌，或"功能先做完、
以后再美化"的潦草页面。

P3 不得覆盖或削弱 P-1、P0、P1、P2；法律真实性和非法律意见原则始终高于视觉设计。

### P3.1 页面完整性

所有用户可见页面必须同时满足：功能完整、视觉完整、交互完整、响应式完整、状态反馈完整。

### P3.2 禁止事项

- 临时页面直接作为正式页面上线；
- 默认组件简单堆叠，不做统一设计；
- 向用户直接展示原始文本、原始 JSON、数据库字段名或开发日志；
- 以"以后再美化""先做功能再说"作为交付理由；
- 每个页面各自使用不同的颜色、圆角、间距或按钮风格；
- 为炫技加入与法律产品无关的复杂动画；
- 使用低对比度、过小字体或难以阅读的布局。

### P3.3 页面气质

专业、严谨、可信、清晰、克制、现代。禁止过度娱乐化、游戏化、赛博朋克或高饱和视觉风格。

### P3.4 设计系统与状态覆盖

必须使用统一的设计系统（颜色、字体、间距、圆角、阴影、响应式断点），组件须覆盖默认、
hover、active、focus、disabled、loading、success、warning、error 等状态；页面须覆盖
loading、empty、error、disabled（及适用时的无权限）状态，不得只实现正常数据状态。

### P3.5 信息呈现顺序

法律内容页面呈现顺序遵循 P1："官方来源 → LawGuard 解释 → 辅助说明"；个案风险提示遵循
P-1，须清晰醒目，不得用小字隐藏在页面角落或使用模糊、诱导性措辞。

## 1. 项目基本信息

- 项目中文名：法护
- 项目英文名：LawGuard
- 项目性质：免费、公益、独立、非商业的刑事诉讼权利普法平台
- 代码仓库根目录：`D:\SOFT\LawGuard`
- 前端目录：`D:\SOFT\LawGuard\web`
- 版本：V1（首个可用版本，范围见第 6 节）

## 2. 核心使命

法护是**面向刑事案件当事人及其家属的公益应急导航平台**。核心价值是让刑事案件当事人
及其家属在关键时刻（例如亲友突然被采取强制措施）知道下一步该怎么办：帮助了解一般性
法定权利、基本程序、文书核对方法和官方救济渠道，降低信息不对称，而不是替代专业法律服务。

> 历史定位说明（不代表当前有效定位，仅保留背景）：V1 早期版本曾以"被讯问、被追诉人员
> 及其家属"为主要表述，2026-07-26 已更新为"当事人及其家属"并明确家属为核心用户之一，
> 详见第 5 节与第 18 节决策记录。当前有效定位以本节及第 5 节为准。

## 3. 公益与非官方性质（永久产品边界）

法护（LawGuard）：

- 纯公益、独立、非商业；永久免费，不收费，V1 不接受捐款；
- 不属于政府机关；不属于公安机关、检察院、法院；不属于律师事务所；不属于法律援助机构；
- 不提供具体案件分析、不提供辩护策略、不判断具体机关或人员是否违法、不替代执业律师；
- 不接受案件委托、不代理案件、不预测案件结果；
- 不承诺取保候审、撤案、无罪或减刑等任何案件结果；
- 不推荐具体律师或律师事务所，不代收律师费，不提供所谓"内部关系"；
- 不主动通过电话、短信、微信、QQ、邮件或任何社交软件联系用户；
- 不要求注册账号，不收集身份证、手机号、银行卡、验证码、案件材料等个人信息；
- 不收集案件信息，不允许用户上传材料，不提供在线咨询，不提供 AI 聊天；
- 提供的均为基于现行法律法规和公开官方来源整理的一般性程序信息，不替代律师针对具体
  案件出具的正式法律意见。

以上边界为长期永久有效原则，任何新功能不得违反。全站统一的公益与防诈骗提示见
`web/src/components/TrustBanner.vue`（第 6 节 12 项）。

## 4. 产品边界

法护只提供**一般性**权利说明、程序指引、文书核对方法和官方救济渠道信息，不针对任何具体案件、具体机关或具体人员发表判断或意见。所有法律相关内容必须来自可核验的公开正式文本，无法核验时明确标注"待法律复核"，不得虚构。

## 5. 目标用户（核心用户，2026-07-26 更新）

被羁押的犯罪嫌疑人、被告人通常无法自由使用手机或访问互联网，因此法护的**直接访问者**
主要是：

- 刑事案件当事人的家属（核心用户之一，不是次要用户）；
- 已经取保候审、监视居住或未被采取强制措施、能够正常访问互联网的当事人本人；
- 被害人及其家属；
- 协助传递法律信息的律师或其他人员；
- 希望了解刑事诉讼程序的普通公众。

对于无法使用互联网的被羁押人员，法护提供适合保存和打印的一般性信息，供家属或律师依法
合规地参考和传递；被羁押人员能否接收、持有或阅读相关材料，应以具体监管场所和办案机关
的现行规定为准，是否能够传递给被羁押人员不由法护决定。不得暗示打印材料一定可以被带入
看守所或监狱，不得虚构监管场所的通信、寄送、会见或材料接收规则；相关表述统一使用
"是否可以寄送、转交或在会见时使用，应以具体监管场所和办案机关的现行规定为准"。

## 6. V1 功能范围

1. 首页
2. 刑事诉讼阶段导航（含详情页，说明案件现在走到哪个程序环节）
3. 权利指引（独立模块，2026-07-26 起与"刑事诉讼阶段导航"拆分为不同页面/路由/内容，
   说明不同阶段一般享有哪些合法权利，不描述程序流程本身，见第 9 节）
4. 法律依据展示
5. 文书核对清单
6. 官方救济渠道
7. 本地全文搜索（计划中，尚未开发，见 `docs/project/AUTODEV_PROGRESS.md` 状态表）
8. 关于项目
9. 隐私说明
10. 使用边界说明
11. 法律来源与版本记录
12. 被羁押后紧急行动指引（2026-07-26 新增确认，P1 重要功能，不取代第 4/11 项等 P0 核心
    模块）：关系身份选择 → 案件/羁押阶段选择 → 是否已有律师 → 生成一般性行动清单；
    覆盖建议联系人顺序、联系不到近亲属时的替代路径（本人提出委托要求/值班律师/法律
    援助）、看守所羁押与监狱服刑差异化说明、防诈骗提醒；联系人记录仅保存在浏览器
    本地（见第 15 节）。实现位置：`web/src/views/EmergencyGuideView.vue` 及
    `web/src/data/emergencyGuidance.ts`、`web/src/composables/useEmergencyContacts.ts`。
13. 全站公益与安全声明（Trust Banner，2026-07-26 新增确认）：首页首屏固定完整版、核心
    页面精简版、打印页面第一页强制版，统一说明纯公益、永久免费、不主动联系、不收集
    个人信息、不推荐律师、不接受案件委托、谨防诈骗。实现位置：
    `web/src/components/TrustBanner.vue`。
14. 打印与保存（2026-07-26 新增确认）：权利指引、诉讼阶段、法律来源、紧急行动指引等
    页面提供"打印或保存本页"入口，复用浏览器原生 `window.print()`，不引入服务端 PDF
    生成；打印版本隐藏导航/按钮/交互控件，保留法律依据、来源、核验日期与免责声明，
    手机号默认脱敏显示。实现位置：`web/src/components/PrintPageButton.vue`、
    `PrintFooter.vue`，全局打印样式见 `web/src/style.css` 的 `@media print` 区块。

各功能的实际代码完成状态（PLANNED / IMPLEMENTED / VERIFIED 等）以
`docs/project/AUTODEV_PROGRESS.md` 的功能状态表为准，本节仅记录"已确认要做/在做"的
长期产品事实，不代表对应代码已经完成。

## 7. V1 明确不做

- 不引入 Java / Spring Boot / Node.js 后端服务；
- 不引入 MySQL / Redis 等数据存储；
- 不做用户系统、登录注册；
- 不做表单提交、文件上传；
- 不做评论、论坛；
- 不做在线咨询、AI 聊天接口；
- 不接入数据统计 SDK、广告 SDK；
- 不引入 Tailwind CSS 或大型 UI 组件库；
- 不做个案分析或辩护策略建议；
- 不接受捐款。

## 8. 信息架构

刑事诉讼阶段（页面信息分类，**不代表对具体案件程序状态作出判断**）：

1. 被带走或被传唤后
2. 讯问过程中
3. 核对和签署文书时
4. 审查起诉阶段
5. 法院审判阶段
6. 判决后及社区矫正阶段

## 9. 页面清单

| 路径 | 页面 | 说明 |
| --- | --- | --- |
| `/` | 首页 | 首屏、阶段入口、核心功能区、官方渠道区、边界说明、页脚 |
| `/stages` | 刑事诉讼阶段导航 | 六个阶段列表，说明"案件现在走到哪个程序环节" |
| `/stages/:id` | 诉讼阶段详情 | 统一详情页模板，见第 12.3 节；不描述具体案件权利，权利内容见权利指引 |
| `/rights-guide` | 权利指引列表 | 按诉讼阶段说明"一般享有哪些合法权利"，与诉讼阶段模块独立维护 |
| `/rights-guide/:id` | 权利指引详情 | 统一详情页模板，与对应诉讼阶段详情互相链接、不重复正文内容 |
| `/documents` | 文书核对清单 | 文书核对方法框架，具体清单内容标注审核状态 |
| `/official-channels` | 官方救济渠道 | 12348、12309 等官方渠道说明与入口占位 |
| `/about` | 关于项目 | 项目定位、公益与非官方性质说明 |
| `/privacy` | 隐私说明 | 不收集用户数据的说明 |
| `/disclaimer` | 使用边界说明 | 不提供个案判断、不替代律师等边界声明 |
| `/legal-sources` | 法律来源与版本记录 | 内容依据的法律文件框架与更新记录 |
| `/emergency-guide` | 被羁押后紧急行动指引 | 3 步引导 + 行动清单结果页，见第 6 节第 12 项 |

`/coming-soon` 统一占位页已于 2026-07-26 随"诉讼阶段详情"功能完成而删除（见第 18 节
决策记录）；未匹配到任何已知路径时统一展示 404 页面（`NotFoundView`），不再需要占位
跳转。

## 10. 内容数据模型

V1 内容以本地 TypeScript 静态数据维护，位于 `web/src/data/`，不接入任何后端或数据库。

`stages.ts` 定义阶段数据结构：

```ts
interface Stage {
  id: string        // 阶段唯一标识
  order: number      // 展示顺序
  name: string        // 阶段名称
  summary: string      // 一句话通俗说明
  status: '待法律复核' | '已复核'  // 内容审核状态，V1 默认均为待法律复核
}
```

## 11. 法律内容审核状态

- 所有法律相关正式内容（法条引用、程序细节、文书清单具体项）在未经执业律师审核前，统一标注为"待法律复核"。
- 禁止仅凭模型记忆虚构法律条文、案号、机关名称或链接。
- 官方渠道链接在未核验前标注"待官方链接核验"，不得描述为已核验的正式官方链接。
- V1 当前所有内容状态：**待法律复核 / 待官方链接核验**（尚未引入任何经核验的正式法律条文或官方链接）。

## 12. UI 设计规范

- 整体风格：冷静、可信、克制、专业，不制造恐慌。
- 主色：深蓝色系（`--color-primary: #14335c`，深蓝强调 `#0b2545`）。
- 辅助色：浅蓝灰（`--color-surface: #eef2f7`）与白色（`--color-bg: #ffffff`）。
- 禁止使用：手铐、铁窗、血迹、警灯、法槌大图、恐怖或对抗性图片、大面积鲜红色、夸张阴影、炫酷动画。
- 移动端优先，适配 375px 宽度，桌面端内容最大宽度约 1200px，不出现横向滚动。
- 不使用外部图片依赖、不使用在线字体、不引入第三方统计脚本。
- 文字清晰、按钮可访问、对比度合理。

### 12.1 Design Tokens（唯一来源：`web/src/style.css`）

全站样式必须复用 `web/src/style.css` 中已定义的 Design Tokens，禁止页面或组件自行
硬编码颜色、字号、间距、圆角、阴影数值。已建立的 token 分类：

- 颜色：`--color-primary(-dark/-light)`、`--color-surface(-alt)`、`--color-bg`、
  `--color-border(-strong)`、`--color-text(-muted/-inverse)`、`--color-notice-*`
  （浅黄色系，仅用于真正需要用户注意的安全提醒）、
  `--color-success/warning/error/info/disabled-*`（低饱和语义色，红色仅用于真正的
  错误/危险场景）；
- 阅读宽度：`--content-width: 760px`，用于说明类页面（关于/文书核对/官方渠道/隐私/
  使用边界/法律来源/诉讼阶段详情/权利指引详情）的标题、导语、列表、段落、提示框统一
  阅读列宽，配合全局 `.prose` 工具类使用（见 `web/src/style.css`），禁止页面各自
  再定义不同的正文最大宽度；
- 圆角：`--radius/-sm/-lg/-pill`；阴影：`--shadow-sm/-md`；
- 间距：`--space-1` ~ `--space-16`（8px 基准 spacing scale）；
- 字体：`--font-size-page-title/section-title/block-title/body/caption/label/metric`；
- 布局：`--max-width: 1200px`（全站唯一内容容器宽度）、`--header-height: 60px`
  （全站唯一 Header 高度来源，`AppHeader.vue` 的高度与定位均引用此变量，禁止重复
  硬编码同一数值）；
- 响应式断点：仅 640px（平板）/ 960px（桌面）两档，全站统一，禁止页面自定义其它
  断点数值。

### 12.2 统一组件职责（禁止混用）

- `TrustBanner`：只用于"纯公益、永久免费、不主动联系、不收集个人信息、防诈骗"这
  一组公益/安全声明，full（首页首屏）/compact（其它核心页面）/print（打印页第一页）
  三种形态，文案统一在组件内维护，页面不得自行改写措辞。
- `LegalDisclaimer`：只用于 P-1"非法律意见"边界声明（不判断案件、不评价机关或
  律师行为、不推荐诉讼/辩护/举报策略），全站唯一来源，页面不得另写含义相近的
  免责文案。
- `NoticeBanner`：只用于一般性提示，默认 `tone="info"`；仅当内容确实构成需要用户
  警惕的安全提醒（例如防诈骗提醒、监管场所规则提醒）时才使用 `tone="caution"`，
  不得把"内容待复核""链接待核验"等纯状态说明也套上警示黄框。
- `PageHeader`：详情页/列表页统一使用，取代各页面自行拼写 `<h1>` + 引导段落。
- `.lead`（`style.css` 全局类）：页面内次级引导段落统一复用，不再各自用相同的
  scoped 样式重复声明。

### 12.3 页面骨架

公开页面默认遵循：Header → Compact/Full Trust Banner → PageHeader（或首页 Hero）→
主要内容 → 次级信息（可折叠） → 边界/免责声明 → Footer。新增页面前应先确认页面
类型（首页 / 列表页 / 详情页 / 步骤页 / 结果页 / 官方渠道页 / 说明页 / 打印页），
复用对应的现有实现作为模板，不自行发明新的页面结构。

## 13. 技术架构

固定技术栈：

- Vue 3 + `<script setup lang="ts">`
- TypeScript
- Vite
- Vue Router（`createWebHistory`）
- 原生 CSS（设计变量 + 组件级样式，无 CSS 框架）
- 静态 TypeScript 内容数据（`web/src/data/`）
- Vitest + @vue/test-utils + jsdom（2026-07-26 新增，前端单元/组件测试）
- Git 版本管理

V1 明确禁止引入的技术见第 7 节。

## 14. 目录结构

以下为主要目录与代表性文件（不逐一列出全部组件），完整列表以实际代码为准；开发前
应先用 `Glob`/`Grep` 等工具核实是否已有同类实现，见第 19 节治理规则。

```
D:\SOFT\LawGuard
├─ CLAUDE.md
├─ LAWGUARD_SOT.md
├─ AUTOMATION_README.md        # Auto Dev 工具自身说明，非产品事实文档
├─ automation/                  # Auto Dev V1 自动开发系统（Python），见 AUTOMATION_README.md
├─ docs/project/
│  └─ AUTODEV_PROGRESS.md       # 唯一开发进度来源
└─ web/
   ├─ index.html
   ├─ vite.config.ts             # 含 Vitest test 配置
   ├─ src/
   │  ├─ main.ts / App.vue / style.css
   │  ├─ router/index.ts
   │  ├─ data/                    # 静态内容数据：stages.ts、legal_sources.ts、
   │  │                             emergencyGuidance.ts 等，各自 __tests__/ 存放单测
   │  ├─ composables/              # 组合式函数，如 useEmergencyContacts.ts（本地存储）
   │  ├─ utils/                    # 纯工具函数，如 phoneMask.ts
   │  ├─ components/                # 通用组件：AppButton/AppCard/AppEmptyState/AppLoading/
   │  │                                AppHeader/AppFooter/AppAccordion/StepProgress/
   │  │                                HeroSection/QuickNavCard/StageCard/FeatureCard/
   │  │                                ChannelCard/NoticeBanner/LegalDisclaimer/PageHeader/
   │  │                                StatusBadge/SourceCitationCard/TrustBanner/
   │  │                                PrintPageButton/PrintFooter/EmergencyContactCard
   │  └─ views/                     # HomeView/StagesView/DocumentsView/
   │                                  OfficialChannelsView/AboutView/PrivacyView/
   │                                  DisclaimerView/LegalSourcesView/EmergencyGuideView/
   │                                  ComingSoonView
   └─ public/
      └─ favicon.svg
```

## 15. 安全与隐私规则

- 不收集任何用户案件材料、个人身份信息；
- 不提供表单提交、文件上传、登录注册；
- 不接入数据统计 SDK、广告 SDK 或任何第三方追踪脚本；
- 不使用 Cookie 进行用户追踪；
- 所有内容均为静态展示，无用户数据在客户端或服务端留存；
- 如某功能确需保存少量非案情数据（例如紧急联系人姓名/关系/手机号/联系状态/备注），
  默认且仅限保存在当前浏览器 `localStorage`，不得上传服务器、不得要求登录，不得采集
  身份证号码、住址或上传任何证件/法律文书；必须提供"一键清除全部本地记录"且清除前
  二次确认；页面须提示"仅保存在当前浏览器，请勿在公共电脑或他人设备上保存敏感信息"；
  localStorage key 须使用项目专属命名空间，避免与其它模块冲突（现有实现见
  `web/src/composables/useEmergencyContacts.ts`，key 为 `lawguard.emergencyContacts.v1`）；
- 打印/导出内容中的手机号等联系方式默认脱敏显示，仅在用户主动勾选后显示完整号码。

## 16. 开发与验收规则

- 每次修改前先阅读本文件；
- 每次完成开发后更新 `docs/project/AUTODEV_PROGRESS.md`（见第 17 节），不得记录到本文件；
- 修改后必须执行 `npm run build`（在 `web/` 目录），确保类型检查与构建均通过；
- 若存在 lint/typecheck 命令需一并执行；
- 验收需确认：TypeScript 无错误、页面可构建、路由刷新不报错、375px 宽度无横向滚动、
  全部页面使用简体中文、不存在表单、不存在用户数据采集、不存在统计 SDK、
  不存在 Vite 默认脚手架内容、不存在被描述为"正式官方"却未核验的链接。

## 17. 开发进度与下一步计划

本文件只保存项目定位、系统架构、设计原则、开发规范、技术路线等长期稳定事实，
不记录逐日开发进度、已完成任务清单或下一步开发计划——这类信息与本文件职责不能重叠。

开发进度、已完成任务（Completed Tasks）、当前任务（Current Task）、下一步候选任务
（Next Candidate Tasks）与已知问题（Known Issues）统一记录在
`docs/project/AUTODEV_PROGRESS.md`，由 Auto Dev 自动开发系统自动维护，请查阅该文件
了解最新进度。

## 18. 重要决策记录

- 2026-07-26：确定 V1 固定技术栈为 Vue 3 + TypeScript + Vite + Vue Router + 原生 CSS + 静态数据，不引入后端、数据库、用户系统。
- 2026-07-26：确定路由采用 `createWebHistory`，尚未完成法律复核的阶段详情统一跳转 `/coming-soon`，不出现 404。
- 2026-07-26：确定 V1 阶段暂定为六个阶段（见第 8 节），仅作为页面信息分类，不代表对具体案件程序状态的判断。
- 2026-07-26：确定所有未经执业律师审核的法律内容统一标注"待法律复核"，官方链接未核验前统一标注"待官方链接核验"，不得虚构。
- 2026-07-26：完成 V1 首页与基础路由页面框架搭建。
- 2026-07-26：确立 "P0：法律内容真实性原则" 为全项目最高原则，法律内容准确性优先级高于开发速度、
  自动化程度与 AI 生成效率；无可靠官方来源时一律停止并输出 `BLOCKED`，不得凭模型记忆编造或推断。
- 2026-07-26：新增 "P-1：非法律意见原则"（优先级高于 P0）、"P1：官方优先原则"、"P2：法律版本
  管理原则"，明确 LawGuard 永久不提供个案法律意见、法律页面必须遵循"官方来源→解释→辅助说明"
  呈现顺序、法律版本变化只能生成待人工审核任务，不得自动覆盖。
- 2026-07-26：目标用户定位更新为"当事人及其家属"，明确家属为核心用户之一，不再将被羁押
  人员本人描述为唯一或主要访问者；新增被害人及其家属、已解除羁押/未被羁押当事人两类用户
  （第 5 节）。旧版"被讯问、被追诉人员及其家属"表述不再作为当前有效定位，仅保留背景说明。
- 2026-07-26：确认新增"被羁押后紧急行动指引"（P1 重要功能）、全站"公益与安全声明
  Trust Banner"、页面打印/保存能力三项长期功能（第 6 节第 12～14 项），实际代码完成
  状态见 `docs/project/AUTODEV_PROGRESS.md` 功能状态表，本条仅记录产品事实已确认。
- 2026-07-26：引入前端单元/组件测试工具链（Vitest + @vue/test-utils + jsdom），`web/`
  新增 `npm run test` 脚本；新增开发前去重检查治理规则（见 `CLAUDE.md`）与本文件第 19 节。
- 2026-07-26：完成首页信息层级重排（Trust Banner 收窄为紧凑条、Hero 改为左右两栏、
  新增"你现在需要什么"任务卡、"使用边界"改为两栏轻量布局、Footer 精简为两层结构），
  并完成全站设计审计：新增 `--header-height` Design Token，`.lead` 引导段落样式
  收编为全局类，删除零引用的 `FeatureCard.vue`，`StageCard`/`ChannelCard` 改用
  `.card--interactive` 与既有 Token（不再自写重复 hover 与魔法数），`StagesView`/
  `DocumentsView`/`OfficialChannelsView`/`AboutView`/`EmergencyGuideView`/
  `PrivacyView` 统一改用 `PageHeader`，`NoticeBanner` 的纯状态说明由
  `tone="caution"` 更正为 `tone="info"`（真正的安全提醒仍保留 caution），
  `AboutView` 与 `LegalDisclaimer` 重叠的免责文案合并去重。详见第 12.1～12.3 节
  与 `docs/project/AUTODEV_PROGRESS.md` 功能状态表。
- 2026-07-26：完成 P0.2 收尾 UI/功能缺口修复：① 删除 `/coming-soon` 占位页与路由，
  六个诉讼阶段全部接入真实详情页（`/stages/:id`，一般性内容，不引用具体法条编号，
  统一标注"待法律复核"）；② 新增"权利指引"独立模块（`/rights-guide`、
  `/rights-guide/:id`），与"诉讼阶段"模块的页面、路由、内容完全拆分，不再共用
  同一路由（此前 `AppHeader` 导航"权利指引"误指向 `/stages`，`QuickNavCard` 存在
  指向 `/rights-guide` 的悬空链接，均已修复）；③ 新增统一详情页模板组件
  `DetailPageLayout.vue`（PageHeader→关键结论→正文→下一步→官方来源→打印→边界
  说明→返回），供诉讼阶段详情与权利指引详情复用；④ 修复首页"使用边界"窄栏文案
  在平板宽度下出现孤字换行、紧急指引第一步提示语被拆开"不/要混用"两处排版问题；
  ⑤ 修复路由懒加载切换/刷新页面时因异步组件无 loading 占位、Footer 短暂贴近
  Header 造成的"蓝色页面一闪而过"视觉跳动（`App.vue` 新增 `Suspense` +
  `AppLoading` 兜底）。详见 `docs/project/AUTODEV_PROGRESS.md` 功能状态表。
- 2026-07-26：排查用户反馈的"刷新页面蓝色闪烁"问题。第一轮把 `index.html`/
  `site.webmanifest` 的 `theme-color` 由深蓝改为白色，修复了浏览器地址栏/
  窗口渲染层面的因素，但用户复测后确认闪烁依然存在，说明这不是根因。第二轮
  用 Playwright + CDP 逐帧采样与录屏定位到真正根因：刷新（reload）后最初
  300～900ms，`<RouterView v-slot>` 的 `Component` 仍为 `undefined`（路由未
  解析完成），`<Suspense>` 不会触发 pending/resolve，`<main>` 高度为 0，深蓝色
  `AppFooter` 已渲染在其后并短暂出现在视口顶部；只在刷新时出现是因为站内跳转
  时目标 chunk 通常已加载，这一空档极短。已在 `App.vue` 用 `!Component ||
  loading` 驱动 `<main>` 的加载态 `min-height`（`Component` 为空或 Suspense
  pending 时都生效）修复，并用 `web/e2e/first-paint-flash.spec.ts`
  （`@playwright/test`，先 `goto` 再 `reload` 复现真实刷新场景）固化回归断言，
  通过 `npm run test:e2e` 单独运行，不影响 `npm run test` 的 Vitest 单测。
- 2026-07-26：建立全站统一正文阅读宽度规范：新增 Design Token
  `--content-width: 760px` 与全局 `.prose` 工具类（第 12.1 节），说明类页面
  与详情页统一接入；明确"h1/h2/Hero 短标题可用 `text-wrap: balance`，普通
  `p`/`li` 一律使用正常换行（`text-wrap: wrap` + `overflow-wrap: break-word` +
  `word-break: normal`），孤字换行改用 `.text-keep` 包裹具体短语解决，不得对
  普通正文整体使用 balance"，修复此前"直接访问者"被错误包裹导致断词的问题。

## 19. 打印与保存原则

- 复用浏览器原生 `window.print()`，不引入服务端 PDF 生成，不开发自定义打印弹窗；
- 打印版本隐藏顶部导航、页脚多余导航、编辑/删除等交互按钮、hover 装饰与其它与正文
  无关的元素；保留标题、正文、法律依据、官方来源、法律版本、最后核验日期、免责声明、
  打印/生成日期与 LawGuard 名称；
- 打印内容黑白清晰，卡片避免跨页截断，链接尽量显示完整网址或保留可识别的官方来源名称；
- 涉及联系方式（如手机号）的打印内容默认脱敏，用户可主动选择显示完整联系方式；
- 全站统一的公益与防诈骗声明在打印页面第一页顶部强制显示，不得隐藏、不得移到页面底部；
- 浏览器打印窗口本身（Print/Destination/Pages/Layout/Color/Cancel 等系统 UI）不属于
  本站 DOM，其界面语言由用户浏览器/系统设置决定，前端不得也无法修改；
- 打印涉及文件名建议时，应确保 `document.title` 与 `<html lang="zh-CN">` 均为中文/
  正确语言标记，避免生成 PDF 默认文件名为英文或语言标记缺失。

## 20. 文档与开发治理规则

- 任何新功能开发前，必须先检查 `LAWGUARD_SOT.md`（本文件）、
  `docs/project/AUTODEV_PROGRESS.md` 和现有代码（路由、页面、组件、组合式函数、工具
  类、数据模型、测试），判断需求属于新功能、已有功能扩展、已有功能修复，还是已完成
  无需修改；如已有相同或高度相似实现，必须复用或修改现有实现，禁止新建重复页面、
  重复组件、重复工具类或重复状态模型。
- 本文件（`LAWGUARD_SOT.md`）记录已经确认的长期产品事实，但**不代表对应代码已经
  完成**；具体功能是否完成，以 `docs/project/AUTODEV_PROGRESS.md` 的功能状态表、
  实际代码、测试结果和 `npm run build`/`npm run test` 结果为准。
- 不得因为新的对话 Prompt（无论来自 ChatGPT 生成的实施指令还是直接的人工指令），再次
  实现已处于"已实现待验证"或"已完成并验证"状态的功能；ChatGPT/人工提供的 Prompt 是
  产品与实施指令，不等于代码完成证明，Claude 的文字总结也不等于完成证明。
- 如新需求与既有功能重叠，应优先扩展或修复现有实现，不得新建重复页面、重复组件或
  重复数据模型。
- 产品定位、核心边界和长期功能发生变化后，必须同步更新本文件；如未形成新的长期产品
  事实，不得为了形式随意修改本文件（对应 `CLAUDE.md` 第 4/9 条）。
- 项目长期只维护 `CLAUDE.md`、`LAWGUARD_SOT.md`、`docs/project/AUTODEV_PROGRESS.md`
  三份文档，不得新增 `PRODUCT_ROADMAP.md`、`FEATURES.md`、`SESSION_SUMMARY.md` 或
  类似重复文档（见 `CLAUDE.md`"文档规则"一节）。

## 21. Product Backlog（未开发高价值方向，2026-07-26 新增）

### 21.0 背景与本节职责边界

2026-07-26 的 Project Audit（口头/对话形式产出，未落盘）识别出多个尚未开发的高价值
方向，但当时没有写入任何可被 Auto Dev Planner 读取的正式文档。此后 Auto Dev 的
Value Gate 升级上线后，Planner 在 Task #14 中把"当前没有满足 Value Gate 的候选任务"
误判为 `BLOCKED`（另见开发历史/Commit 记录中的 NO_HIGH_VALUE_TASK 状态修复）；根因
除了状态语义混淆之外，还包括 Planner 完全没有一份权威的"已确认高价值缺口清单"可以
消费，只能临时在多个已饱和模块（打印按钮/aria-label 等）里打转。本节是该清单的
唯一权威来源。

**职责边界**（与 `docs/project/AUTODEV_PROGRESS.md` 的既有分工一致，见第 17 节）：
本节只记录"已确认要不要做、优先级多高、范围是什么"（长期产品范围事实），不记录
"哪次 Auto Dev 运行、什么时候完成了哪个切片"——后者仍然只能写入
`docs/project/AUTODEV_PROGRESS.md`。每个 Backlog 条目的 `状态` 字段只反映"当前是否
可以被规划"（READY/IN_PROGRESS/DONE/BLOCKED/DEFERRED），不是执行时间线。

**与第 6 节"V1 功能范围"的关系**：本节列出的方向部分已经出现在第 6 节（如
第 7 项"本地全文搜索"），部分尚未出现——本节本身即是 `CLAUDE.md` 开发规则第 9 条
要求的"新功能需求先在 LAWGUARD_SOT.md 中确认"这一步；条目一旦被本节列为
READY/IN_PROGRESS/DONE，即视为已确认纳入 V1 范围，无需再单独修改第 6 节列表
（后续如产出实际代码，应同步更新第 6 节，避免两处表述不一致）。

**必须遵守 P-1/P0**：任何 Backlog 条目的实现都不得让系统判断案件是否合法/是否
构成犯罪/可能判几年，不得虚构具体机构名称、电话、地址等未经核验的法律/联系方式
事实；涉及具体法律来源核验的条目在没有可核验来源前必须保持 `BLOCKED`，不得为了
推进 Backlog 而编造。

### 21.1 优先级原则

围绕核心使命排序（帮助被讯问、被追诉人员及家属，在法律框架内理解权利、核对程序、
找到正规救济路径）：

- **P0**：直接提升用户在紧急场景下的可执行性、权利理解和官方求助能力。
- **P1**：提升内容查找、可及性、离线使用和易读性。
- **P2**：非核心增强或技术预研。
- PrintButton、aria-label、间距、图标、普通 CSS 微调等不得进入本 Backlog（这类改动
  应通过既有 Value Gate 的重复检测规则默认拒绝，见 `automation/value_gate.py`）。

### 21.2 Backlog 条目

#### BL-001：交互式个人处境导航工具

- **用户问题**：当事人/家属不知道自己（或亲属）的情况大致对应哪个诉讼阶段，应该
  先看哪个权利指引、哪份文书核对清单，只能自己在六个阶段页面里逐个排查。
- **功能目标**：基于已有 `stages.ts`/`rightsGuide.ts` 静态数据，提供一个纯客户端的
  引导式问答（例如"是否已被讯问/是否已被起诉/是否已开庭/是否已判决"），把回答
  映射到对应的诉讼阶段详情页与权利指引详情页入口，帮助用户快速定位该看哪个页面。
- **核心价值**：直接提升紧急场景下的可执行性与权利理解（P0）。
- **范围**：纯前端状态机 + 已有数据映射，不新增法律内容，不新增数据源；结果页
  必须包含与 `StagesView`/`EmergencyGuideView` 一致的"仅为一般性信息分类，不代表
  对具体案件程序状态或法律后果作出判断"声明（对应 P-1）。
- **非目标**：不判断案件是否合法/是否构成犯罪/可能判几年；不生成个案化建议；
  不替代律师意见；不存储用户输入（除非复用 `useEmergencyContacts.ts` 已有的
  "仅保存在浏览器本地"模式，且需在 `developer_prompt` 中显式要求）。
- **风险**：若问答设计不当可能让用户误以为在获得"个案诊断"，必须在文案与
  `developer_prompt` 中明确禁止诊断式措辞，验收标准需包含"不出现个案判断类表述"
  的人工检查项。
- **状态**：READY
- **优先级**：P0
- **是否允许 Auto Dev**：是（第一切片允许；如后续切片涉及新的法律事实判断规则，
  应重新评估）
- **验收标准**：问答流程可在 3 步内到达结果页；结果页展示至少 1 个阶段入口与 1 个
  权利指引入口的可点击链接；包含免责声明；375px 宽度无横向滚动；覆盖
  loading/empty/error 状态；`npm run build` 通过。
- **依赖**：无（复用现有 `web/src/data/stages.ts`、`web/src/data/rightsGuide.ts`）。

#### BL-002：官方求助渠道省级查询入口（骨架，不新增未核验联系方式）

- **用户问题**：`OfficialChannelsView` 目前只展示全国性的 12348/12309 等渠道，
  用户不知道自己所在省份具体应该联系哪个司法行政机关/法律援助中心。
- **功能目标**：在 `OfficialChannelsView` 新增"按省份筛选"的交互骨架：用户选择
  省份后，页面展示已核验的全国性渠道（12348 中国法律服务网热线、12309 检察服务
  热线等，复用现有内容），并为每个省份预留一个标注"待官方链接核验"的占位条目，
  明确说明"具体机构名称/电话正在核验中，请以 12348/12309 转接为准"。
- **核心价值**：直接提升官方求助能力（P0）。
- **范围**：前端筛选交互 + 占位数据结构，不得新增任何未经核验的具体机构名称、
  电话、地址（对应 P0.2）。
- **非目标**：不在本切片内编造/推断各省具体法律援助中心联系方式；不做地理定位。
- **风险**：如果 Auto Dev 或后续人工在无来源情况下"顺手"填入具体机构信息，直接
  违反 P0，必须在 `developer_prompt` 与验收标准中显式禁止。
- **状态**：READY（骨架部分）；具体各省机构信息填充部分为 **BLOCKED**（见依赖）。
- **优先级**：P0
- **是否允许 Auto Dev**：是（仅骨架/占位部分；具体机构信息填充需人工提供可核验
  来源后才能解除 BLOCKED，见 `LAWGUARD_SOT.md` P0.2 允许来源清单）。
- **验收标准**：省份选择控件可用；未核验省份统一显示"待官方链接核验"状态组件
  （复用现有状态组件，不新造样式）；全国性渠道内容与现状一致，未被误改；
  `npm run build` 通过。
- **依赖**：各省具体机构名称/电话的可核验官方来源（当前不存在，填充前必须停止
  并输出 BLOCKED，不得编造）。

#### BL-003：本地全文搜索

- **用户问题**：用户记得某个关键词（如"取保候审"）但不知道该词出现在哪个页面。
- **功能目标**：对已发布的静态内容（阶段、权利指引、法律来源、官方渠道等）建立
  本地索引，提供搜索入口与结果列表，支持关键词高亮与结果跳转，覆盖无结果状态。
- **核心价值**：提升内容查找与可及性（P1）；已是第 6 节 V1 功能范围第 7 项
  "计划中，尚未开发"的既有确认事项，本条目为其正式拆解。
- **范围**：纯前端本地索引（构建时或运行时对已有 `web/src/data/*.ts` 内容建立
  索引），不接入任何后端搜索服务、不引入统计/日志 SDK。
- **非目标**：不做模糊语义搜索/AI 问答式检索（V1 不做 AI 聊天接口）。
- **风险**：索引需覆盖尚未核验内容时，搜索结果仍须保留"待法律复核"等既有状态
  标注，不得在搜索结果里丢失这些标注。
- **状态**：READY
- **优先级**：P1
- **是否允许 Auto Dev**：是，须按第 21.3 节拆分为多个垂直切片。
- **验收标准**：见第 21.3 节切片验收标准。
- **依赖**：无。

#### BL-004：简明语言 / 大字版模式

- **用户问题**：部分家属（尤其年长者）对当前字号、术语密度感到阅读困难。
- **功能目标**：新增一个可全局切换的"大字版"显示模式（提升正文字号与行高，
  复用现有 Design Tokens 做等比放大），不新增独立的"简明语言"文案分支（避免
  维护两套法律内容，产生 P0 一致性风险）。
- **核心价值**：提升可及性/易读性（P1）。
- **范围**：纯 CSS/Design Token 层面的显示模式切换，不修改任何法律内容文本。
- **非目标**：不重写文案为"简化版法律内容"（避免同一事实存在两个版本、难以
  同步核验状态，违反 P0 单一事实来源要求）。
- **风险**：需确保切换状态可被键盘/屏幕阅读器识别（对应无障碍要求）。
- **状态**：READY
- **优先级**：P1
- **是否允许 Auto Dev**：是。
- **验收标准**：提供可发现的切换入口；切换后正文字号提升且不破坏布局；375px
  宽度无横向滚动；覆盖 loading/empty/error 状态显示（大字版下同样正常）；
  `npm run build` 通过。
- **依赖**：无。

#### BL-005：PWA 离线支持

- **用户问题**：用户在看守所/法院等信号不佳场所无法访问已经看过的页面内容。
- **功能目标**：新增 Web App Manifest 与 Service Worker，缓存已发布的静态页面
  （阶段、权利指引、官方渠道、紧急指引等只读内容），支持离线重新访问。
- **核心价值**：提升离线可用性（P1）。
- **范围**：纯前端构建产物缓存策略，不缓存/不影响任何表单提交（V1 本身无表单）。
- **非目标**：不做后台推送通知；不做离线数据同步（无用户数据可同步）。
- **风险**：需明确离线状态提示，避免用户误以为离线内容是"实时更新的官方信息"。
- **状态**：READY
- **优先级**：P1
- **是否允许 Auto Dev**：是，须按第 21.3 节拆分为多个垂直切片。
- **验收标准**：见第 21.3 节切片验收标准。
- **依赖**：无。

#### BL-006：Legal Sources 法律来源内容深化核验

- **用户问题**：`Legal Sources` 页面内容层面完成度约 60%，多项来源尚未标注可核验
  官方出处。
- **功能目标**：为现有法律依据条目补全官方来源链接、发布日期、最后核验日期。
- **核心价值**：直接提升权利理解的可信度（P0）。
- **范围**：仅补全可核验来源的条目，不推断/编造条文内容。
- **非目标**：不新增法律解释性文字（除非同样有可核验来源支撑）。
- **风险**：无来源硬填会直接违反 P0，是本条目当前无法自动推进的唯一原因。
- **状态**：BLOCKED（缺少人工提供的可核验官方来源，见 P0.2 允许来源清单）
- **优先级**：P0
- **是否允许 Auto Dev**：否，直到有人工提供具体来源清单为止。
- **验收标准**：（待来源确定后另行制定）
- **依赖**：人工提供官方公开文本/官方门户链接。

#### BL-007：Documents 文书核对清单内容扩充

- **用户问题**：`DocumentsView` 目前仅有通用框架（完成度约 50%），具体清单条目
  不足以覆盖常见文书核对场景。
- **功能目标**：在已核验的法律依据基础上扩充具体文书核对条目。
- **核心价值**：提升程序核对的可执行性（P0，因该功能直接服务"核对程序"这一
  核心使命环节）。
- **范围**：仅在已有法律依据可核验时新增条目。
- **非目标**：不生成个案化的文书审查意见。
- **风险**：与 BL-006 相同的来源风险。
- **状态**：BLOCKED（同 BL-006，缺少可核验的法律依据来源清单）
- **优先级**：P0
- **是否允许 Auto Dev**：否，直到有人工提供具体条目来源为止。
- **验收标准**：（待来源确定后另行制定）
- **依赖**：人工提供官方公开文本/官方门户链接。

#### BL-008：首屏性能优化技术预研

- **用户问题**：Capability Matrix 显示 Performance 约 70%，尚未系统性测量/优化。
- **功能目标**：引入构建产物体积分析、关键渲染路径测量，输出优化建议清单（本
  条目本身不直接产出面向用户的新功能）。
- **核心价值**：非核心增强/技术预研（P2）。
- **状态**：DEFERRED（P0/P1 条目未清空前不应优先安排）
- **优先级**：P2
- **是否允许 Auto Dev**：是，但排在 P0/P1 之后。
- **验收标准**：（待安排时另行制定）
- **依赖**：无。

### 21.3 大功能垂直切片拆分（BL-003 本地全文搜索 / BL-005 PWA 离线支持示例）

大型 Backlog 条目必须拆成有独立业务意义、可验证的垂直切片，禁止拆成无意义的
页面按钮复制/图标调整/间距微调。每个切片完成后用户必须获得可观察的新能力，或
明确推进到可用状态。

**BL-003 本地全文搜索**建议切片（按顺序）：

1. `BL-003-1` 建立内容索引：对 `web/src/data/*.ts` 已发布内容建立本地搜索索引
   数据结构。验收：索引可在构建/运行时生成，覆盖阶段/权利指引/法律来源/官方渠道
   至少 4 类内容。
2. `BL-003-2` 提供搜索入口与结果列表：新增搜索输入框与结果列表页/组件，复用
   现有 Design Tokens 与卡片组件。验收：输入关键词后展示匹配结果标题与所属页面。
3. `BL-003-3` 支持关键词高亮与结果跳转：结果中高亮命中关键词，点击跳转到对应
   页面/锚点。验收：高亮准确、跳转后可见对应内容。
4. `BL-003-4` 无结果状态与测试：覆盖空结果提示、补充单元测试。验收：无结果时
   展示统一的 empty 状态组件；新增至少 1 组单元测试覆盖索引与结果匹配逻辑。

**BL-005 PWA 离线支持**建议切片（按顺序）：

1. `BL-005-1` 接入 Web App Manifest：新增/完善 manifest 文件与图标声明。
2. `BL-005-2` 接入 Service Worker 基础缓存：缓存已发布静态页面的构建产物。
3. `BL-005-3` 离线状态提示：网络不可用时明确提示"当前为离线缓存内容"。
4. `BL-005-4` 离线场景测试：新增测试验证核心页面离线可访问。

以上切片编号仅为 Backlog 层面的规划标识，不是 Git 分支/Commit 编号；Planner
生成任务时应在任务字段中注明来源 `backlog_id`（见第 21.4 节）与当前对应切片。

### 21.4 Planner 消费方式（摘要，详见 `automation/backlog.py`）

Auto Dev Planner 每次运行必须先读取本节 Backlog：存在状态为 READY 且允许 Auto
Dev 的条目时，应优先从中选择优先级最高者规划第一个可交付切片，不得绕过 Backlog
临时制造低价值任务，也不得在此情况下返回 `NO_HIGH_VALUE_TASK`。只有当 Backlog
中不存在任何 READY 条目、也没有可自动解除的 BLOCKED 条目、且没有发现新的高价值
缺口时，才允许返回 `NO_HIGH_VALUE_TASK`。生成的任务须在字段中标明来源
`backlog_id`、当前交付切片、为什么该切片推进了完整功能、完成后 Backlog 是否仍有
剩余工作。具体实现见 `automation/backlog.py` 与 `automation/orchestrator.py`。
